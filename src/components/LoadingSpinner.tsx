export function LoadingSpinner() {
  return (
    <div
      className="inline-block size-8 animate-spin rounded-full border-2 border-gray-200 border-t-indigo-600"
      role="status"
      aria-label="Loading"
    />
  );
}
